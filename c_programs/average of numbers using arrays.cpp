#include<stdio.h>
int main()
{
int a[5],i,sum=0;	
	float average;
	printf("Enter five numbers");
	scanf("%d",&a[i]);
	for(i=0;i<=4;i++)
	 sum=sum+a[i];
	 average=sum/5.0;
	 printf("average is%f",average);
	 
	 
}

